# Frontend environment variables (copy to `.env.local` and edit as needed)

# Backend API (FastAPI mock server)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Alternative frontend API var used by some routes
NEXT_PUBLIC_API_URL=http://localhost:8000

# Explorer base used by `lib/starknet-client.ts`
NEXT_PUBLIC_EXPLORER_BASE=https://sepolia.starkscan.co/tx

# Optional: Supabase / Postgres used by backend (frontend may also use Supabase client)
# Set these only if you want the frontend to interact with Supabase directly
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional relayer (if you have a relayer service for real on-chain tx submission)
STARKNET_RELAYER_URL=
STARKNET_RELAYER_KEY=

# Optional: Postgres / Supabase connection string for backend persistence.
# If set, the backend will persist data (agents, keys, listings, actions, proofs, schedules).
# Example (Supabase direct URL):
# DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@db.zokwvjntsadeykcidvre.supabase.co:5432/postgres

# Note: Backend settings live in `backend/.env`. Configure `MOCK_BLOCKCHAIN=true` for demo mode,
# or `false` to enable real StarkNet RPC and relayer flows. Keep all secret keys out of source control.
